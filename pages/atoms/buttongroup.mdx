import { ButtonGroup } from '@/atoms';
import { Button } from '@/primals';

# Button Group

<ButtonGroup><Button variant="danger">a</Button><Button variant="info">b</Button><Button variant="success">c</Button></ButtonGroup>
